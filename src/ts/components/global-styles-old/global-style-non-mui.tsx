import React from "react";
import ReactDOM from "react-dom";
import { ThemeProvider } from "styled-components";

export const themeNonMui = {
  colors: {
    lightest: "white",
    secondary: "red",
    primary: "yellow"
  }
};


